export function my_add(a: number, b: number): number {
  return a + b;
}

export function my_add_4(a: number, b: number): number {
  return a + b;
}

export function my_multiply(a: number, b: number): number {
  return a * b;
}
